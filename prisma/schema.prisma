generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model test {
  hello String @unique @db.VarChar(50)
}

model othertest {
  id Int @id @default(autoincrement())
  description String @unique @db.VarChar(50)
}
